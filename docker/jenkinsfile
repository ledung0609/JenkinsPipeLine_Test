pipeline {
    agent{
    	label 'agent'
    }
    
    stages {
        stage('Initialize') {
            steps {
                sh java-version
                echo 'こんちは！'
                sh 'pwd'
            }
        }
    }
}
