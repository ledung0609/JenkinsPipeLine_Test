def COLOR

pipeline {
    agent{
    	label 'agent_2'
    }
    
    stages {
        stage('Initialize') {
            steps {
                sh 'pwd'
                sh 'dir JenkinsAgentForDockerContainer'
                sh 'cd docker'
                sh 'dir'
            }
        }
    }
}
