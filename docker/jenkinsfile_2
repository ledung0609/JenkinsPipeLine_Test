def COLOR

pipeline {
    agent{
    	label 'agent'
    }
    
    stages {
        stage('Initialize') {
            steps {
                sh 'pwd'
                sh 'cd docker'
                sh 'dir'
            }
        }
    }
}
