def COLOR

pipeline {
    agent{
    	label 'agent'
    }
    
    stages {
        stage('Initialize') {
            steps {
                sh 'pwd'
                sh 'dir agent'
                sh 'cd docker'
                sh 'dir'
            }
        }
    }
}
