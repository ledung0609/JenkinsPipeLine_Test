def COLOR

pipeline {
    agent{
    	label 'agent_2'
    }
    
    stages {
        stage('Initialize') {
            steps {
                sh 'pwd'
                sh 'java -version'
                sh 'dir'
                sh 'cd docker'
            }
        }
    }
}
