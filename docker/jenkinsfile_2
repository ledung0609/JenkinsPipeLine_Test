def COLOR

pipeline {
    agent{
    	label 'agent_2'
    }
    
    stages {
        stage('Initialize') {
            steps {
                sh 'pwd'
                sh 'dir'
                sh 'cd docker'
                sh 'dir'
            }
        }
    }
}
